SBT_DIR=$OPENSHIFT_DATA_DIR/.sbt
IVY_DIR=$OPENSHIFT_DATA_DIR/.ivy

cd $OPENSHIFT_REPO_DIR

$OPENSHIFT_REPO_DIR/sbt -sbt-launch-dir $SBT_DIR -sbt-dir $SBT_DIR -ivy $IVY_DIR -Dfile.encoding=utf8 -J-Xmx512m run
